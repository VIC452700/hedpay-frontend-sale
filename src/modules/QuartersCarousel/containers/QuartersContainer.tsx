import React from "react";

// import { useHdpSaleContract } from "@hedpay/hooks/useHdpSaleContract";
import QuartersView from "../views/QuartersView";

export const QuartersContainer = () => {
  // const { hdpUserAmount, account, getUserAmount } = useHdpSaleContract();

  // useEffect(() => {
  //   getUserAmount(account || "");
  // }, [account, getUserAmount]);

  return <QuartersView />;
};
